"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.startUserLoginServer = void 0;
const axios_1 = __importDefault(require("axios"));
const http_1 = __importDefault(require("http"));
const url_1 = __importDefault(require("url"));
const config_util_1 = require("./utils/config.util");
const env_util_1 = require("./utils/env.util");
const logger_util_1 = __importStar(require("./utils/logger.util"));
const LOCAL_SERVER_PORT = 42069;
const corsHeaders = {
    'Access-Control-Allow-Origin': '*' /* @dev First, read about security */,
    'Access-Control-Allow-Methods': 'OPTIONS, POST, GET',
    'Access-Control-Max-Age': 2592000, // 30 days
    'Access-Control-Allow-Headers': 'Content-Type, Authorization, baggage, sentry-trace',
};
// Start the server function
function startUserLoginServer(onStarted, onUserLoggedIn) {
    const server = http_1.default.createServer((req, res) => __awaiter(this, void 0, void 0, function* () {
        var _a;
        if (req.url === '/status') {
            res.writeHead(200, Object.assign({ 'Content-Type': 'text/plain' }, corsHeaders));
            res.end('ok');
            return;
        }
        logger_util_1.default.dev.log('req.url', req.url);
        const token = (_a = url_1.default.parse(`localhost:${LOCAL_SERVER_PORT}${req.url}`, true)
            .query) === null || _a === void 0 ? void 0 : _a.token;
        logger_util_1.default.dev.log('token', token);
        let user;
        if (token) {
            const res = yield axios_1.default
                .get(`${env_util_1.MKDB_BASE_URL}/api/users`, {
                headers: {
                    Authorization: `${token}`,
                },
            })
                .catch((err) => {
                logger_util_1.default.log('err', err.response.data);
                return undefined;
            });
            user = res === null || res === void 0 ? void 0 : res.data;
            logger_util_1.default.dev.logJson(res === null || res === void 0 ? void 0 : res.data);
        }
        res.writeHead(200, { 'Content-Type': 'text/plain' });
        res.end("nice, you're logged in muchacho.  you can go back to your CLI now friend.");
        // Optionally, you can close the server when a response is sent
        server.close();
        onUserLoggedIn === null || onUserLoggedIn === void 0 ? void 0 : onUserLoggedIn(user);
        if (user) {
            logger_util_1.default.log(`\nLogged in as: ${(0, logger_util_1.withColor)(user.name)}`);
            (0, config_util_1.writeGlobalCliConfig)({ user: Object.assign(Object.assign({}, user), { apiToken: token }) });
        }
    }));
    server.listen(LOCAL_SERVER_PORT, () => {
        onStarted();
    });
}
exports.startUserLoginServer = startUserLoginServer;
