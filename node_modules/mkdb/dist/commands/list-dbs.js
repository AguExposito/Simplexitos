"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const axios_1 = __importDefault(require("axios"));
const config_util_1 = require("../utils/config.util");
const env_util_1 = require("../utils/env.util");
const logger_util_1 = __importDefault(require("../utils/logger.util"));
const login_1 = __importDefault(require("./login"));
const listDatabases = () => __awaiter(void 0, void 0, void 0, function* () {
    let userInfo = yield (0, config_util_1.getLoggedInUser)();
    if (!userInfo) {
        yield (0, login_1.default)();
        userInfo = yield (0, config_util_1.getLoggedInUser)();
        if (!userInfo)
            return logger_util_1.default.log('you must be logged in to create a database');
    }
    // postgresql://wghwodiz:huankpxhunyarojqzgmb@alpha.europe.mkdb.sh:5432/nmufathh
    const res = yield axios_1.default
        .get(`${env_util_1.MKDB_BASE_URL}/api/databases`, {
        headers: { Authorization: userInfo.apiToken },
    })
        .catch((error) => {
        logger_util_1.default.log('error', error);
    });
    if (res === null || res === void 0 ? void 0 : res.data) {
        logger_util_1.default.dev.log(`list database res`, res.data);
        const cleanedData = res.data.map((db) => ({
            db: db.name,
            id: db.id,
            connect: `${db.provider === 'POSTGRES' ? 'postgresql' : 'unknown'}://${db.username}:${db.auth_UNSAFE.password}@${db.host}:${db.port}/${db.dbName}`,
        }));
        logger_util_1.default.table(cleanedData);
    }
    else {
        logger_util_1.default.log('error fetching databases..');
    }
    return null;
});
exports.default = listDatabases;
