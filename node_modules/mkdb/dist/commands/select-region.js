"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.getRegionChoice = void 0;
const config_util_1 = require("../utils/config.util");
const logger_util_1 = __importStar(require("../utils/logger.util"));
const login_1 = __importDefault(require("./login"));
var Radio = require('prompt-radio');
const selectRegion = (...args_1) => __awaiter(void 0, [...args_1], void 0, function* (deafen = false) {
    const log = deafen ? () => { } : logger_util_1.default.log;
    const prevConfig = (0, config_util_1.getGlobalCliConfig)();
    let userInfo = prevConfig === null || prevConfig === void 0 ? void 0 : prevConfig.user;
    if (!userInfo) {
        yield (0, login_1.default)();
        userInfo = yield (0, config_util_1.getLoggedInUser)();
        if (!userInfo)
            return log('you must be logged in to perform this action');
    }
    const regionChosen = yield (0, exports.getRegionChoice)(userInfo);
    if (!regionChosen)
        return log('action cancelled');
    log('\nupdating user settings...');
    const userUpdateRes = yield (0, config_util_1.updateUserDataAndPersist)({
        defaultRegion: regionChosen.region,
    });
    if (userUpdateRes === 200) {
        log(`\ndefault db region - ${regionChosen.label} - saved successfully`);
        return regionChosen;
    }
});
exports.default = selectRegion;
const REGIONS = [
    { label: 'americas (miami)', region: 'US_EAST' },
    { label: 'asia (delhi)', region: 'INDIA' },
    { label: 'europe (frankfurt)', region: 'EU_CENTRAL' },
];
const getRegionChoice = (userInfo) => __awaiter(void 0, void 0, void 0, function* () {
    const defaultIndex = REGIONS.findIndex((r) => r.region === userInfo.defaultRegion);
    const regionSelectLabels = REGIONS.map((r, i) => r.label + (i === defaultIndex ? (0, logger_util_1.withColor)(' current ', 'cyan') : ''));
    const defaultLabel = defaultIndex >= 0 ? regionSelectLabels[defaultIndex] : undefined;
    const regionLabelChosen = yield new Radio({
        name: 'colors',
        message: `\nchoose region (space to select, enter to submit)`,
        choices: regionSelectLabels,
        default: defaultLabel,
    }).run();
    const regionChosen = REGIONS.find((r) => r.label === regionLabelChosen);
    return regionChosen;
});
exports.getRegionChoice = getRegionChoice;
