"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const axios_1 = __importDefault(require("axios"));
const copy_paste_1 = __importDefault(require("copy-paste"));
const config_util_1 = require("../utils/config.util");
const env_util_1 = require("../utils/env.util");
const logger_util_1 = __importDefault(require("../utils/logger.util"));
const login_1 = __importDefault(require("./login"));
const logout_1 = __importDefault(require("./logout"));
const select_region_1 = __importDefault(require("./select-region"));
const createDb = (dbname) => __awaiter(void 0, void 0, void 0, function* () {
    var _a;
    let userInfo = yield (0, config_util_1.getLoggedInUser)();
    if (!userInfo) {
        yield (0, login_1.default)();
        userInfo = yield (0, config_util_1.getLoggedInUser)();
        if (!userInfo)
            return logger_util_1.default.log('you must be logged in to create a database');
    }
    let region = userInfo.defaultRegion || ((_a = (yield (0, select_region_1.default)(true))) === null || _a === void 0 ? void 0 : _a.region);
    console.log('region', region);
    const res = yield axios_1.default
        .post(`${env_util_1.MKDB_BASE_URL}/api/databases`, { name: dbname, region }, {
        headers: {
            Authorization: userInfo.apiToken,
            'Content-Type': 'application/json',
        },
    })
        .catch((error) => {
        // TODO:  if user's token has expired, we should gracefully re-login, not really handling 401 well right now
        logger_util_1.default.dev.log('error:', [
            error.response.status,
            error.response.statusText,
        ]);
        if (error.response.status === 401) {
            logger_util_1.default.log('looks like your session has expired. please login again.\n\nmkdb login');
            (0, logout_1.default)(true);
            logger_util_1.default.log('error', error.response.data.message);
        }
        else if (error.response.status === 402) {
            logger_util_1.default.log(`db limit exceeded`);
        }
        return undefined;
    });
    logger_util_1.default.dev.log('res', res === null || res === void 0 ? void 0 : res.data);
    if (res === null || res === void 0 ? void 0 : res.data) {
        const logConnString = (wasCopied) => logger_util_1.default.log(`\nconnection string${wasCopied ? ' (copied to clipboard)' : ''}:\n${res.data.connectionString}`);
        logger_util_1.default.log(res.data.message);
        try {
            copy_paste_1.default.copy(res.data.connectionString);
            logConnString(true);
        }
        catch (err) {
            logConnString(false);
        }
    }
    return null;
});
exports.default = createDb;
