"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
const login_server_1 = require("../login-server");
const config_util_1 = require("../utils/config.util");
const env_util_1 = require("../utils/env.util");
const logger_util_1 = __importStar(require("../utils/logger.util"));
const login = () => __awaiter(void 0, void 0, void 0, function* () {
    const currentConfig = (0, config_util_1.getGlobalCliConfig)();
    if (currentConfig === null || currentConfig === void 0 ? void 0 : currentConfig.user) {
        logger_util_1.default.log(`already logged in as: ${(0, logger_util_1.withColor)(currentConfig.user.name)}`);
        logger_util_1.default.dev.logJson(currentConfig);
        return currentConfig.user;
    }
    return new Promise((resolve) => {
        (0, login_server_1.startUserLoginServer)(() => {
            var url = `${env_util_1.MKDB_BASE_URL}/login/cli`;
            logger_util_1.default.log(`opening browser: ${url}`);
            var start = process.platform == 'darwin'
                ? 'open'
                : process.platform == 'win32'
                    ? 'start'
                    : 'xdg-open';
            require('child_process').exec(start + ' ' + url);
            logger_util_1.default.log('\nwaiting for login..');
        }, (user) => resolve(user));
    });
});
exports.default = login;
